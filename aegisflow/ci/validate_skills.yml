name: Validate Skills

on:
  push:
    paths:
      - 'aegisflow/skills/**'
  pull_request:
    paths:
      - 'aegisflow/skills/**'

jobs:
  validate-skills:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r aegisflow/requirements.txt

      - name: Validate skills
        run: python aegisflow/scripts/validate_skills.py

      - name: Detect orphan skills
        run: python aegisflow/scripts/detect_orphans.py

      - name: Enforce versioning
        if: github.event_name == 'pull_request'
        run: |
          python aegisflow/scripts/enforce_versioning.py \
            --base-ref origin/${{ github.base_ref }}
